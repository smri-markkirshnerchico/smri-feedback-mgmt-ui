import { CompanyView } from "src/sections/organization/company/view/company-view";

// ----------------------------------------------------------------------

export const metadata = { title: `Company` };

export default function Page() {
    return <CompanyView />;
}
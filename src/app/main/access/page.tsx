import { AccessView } from "src/sections/access/view/access-view";

// ----------------------------------------------------------------------

export const metadata = { title: `Access` };

export default function Page() {
  return <AccessView />;
}
import { ModuleView } from "src/sections/module/view/module-view";

// ----------------------------------------------------------------------

export const metadata = { title: `Module` };

export default function Page() {
  return <ModuleView />;
}
import { ApplicationView } from "src/sections/application/view/application-view";

// ----------------------------------------------------------------------

export const metadata = { title: `Application` };

export default function Page() {
  return <ApplicationView />;
}
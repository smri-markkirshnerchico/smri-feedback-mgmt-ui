import { LovView } from "src/sections/lov/view/lov-view";

// ----------------------------------------------------------------------

export const metadata = { title: `List of Values` };

export default function Page() {
    return <LovView />;
}
import { AdminDashboardView } from 'src/sections/admin';

// ----------------------------------------------------------------------

export const metadata = { title: `Admin Dashboard` };

export default function Page() {
  return <AdminDashboardView />;
}
